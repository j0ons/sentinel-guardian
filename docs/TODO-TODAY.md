# Do these today (June 9) — Week 0 setup

Order matters: credit form first (approval lags), then unblock the build.

1. [ ] **Qwen Cloud: sign up + submit hackathon credit-coupon form.** ← do FIRST.
   - Then generate an API key. Paste it into `.env` as `QWEN_API_KEY=...` (file is gitignored).
2. [ ] **Devpost:** open https://qwencloud-hackathon.devpost.com/ → "Join hackathon" (account `j0ons`).
3. [ ] **Join the Qwen Cloud Discord** (link on the Resources page).
4. [ ] **Order a Raspberry Pi 5** + camera module + 32GB+ SD + USB-C PSU. (~$80–120.)
5. [ ] Tell me your API key is set → I'll write & run `src/qwen_client.py` (first real Qwen call =
   our proof-of-deployment file) and confirm tool-calling + embeddings work.

Once #1 and #5 are done, we hit **GATE 0** and Week 1 (the vertical slice) begins.
